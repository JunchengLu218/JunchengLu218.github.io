# 网站编辑指南

给 `junchenglu-homepage` 项目。整个网站只有两个文件需要你动：

| 文件 | 管什么 |
| --- | --- |
| `index.html` | **所有内容**（文字、论文、项目、经历、链接、示意图） |
| `stylesheet.css` | **所有外观**（字体、字号、颜色、间距、深浅色模式） |

其余文件：`images/profile.jpg`（头像）、`data/`（CV PDF）、`favicon.svg`（浏览器标签图标）、`README.md`（英文说明，给别人看的）。

没有构建步骤，没有依赖。改完存盘，刷新浏览器就能看到。

---

## 目录

1. [怎么预览](#1-怎么预览)
2. [为什么之前"轻浮"——设计原则](#2-为什么之前轻浮设计原则)
3. [调外观：stylesheet.css](#3-调外观stylesheetcss)
4. [改内容：index.html](#4-改内容indexhtml)
5. [常见任务速查](#5-常见任务速查)
6. [示意图（SVG）怎么换](#6-示意图svg怎么换)
7. [发布到 GitHub Pages](#7-发布到-github-pages)
8. [出错了怎么办](#8-出错了怎么办)

---

## 1. 怎么预览

打开终端：

```bash
cd ~/Documents/Github/junchenglu-homepage && python3 -m http.server 8777
```

浏览器打开 <http://localhost:8777>。改完文件按 `Cmd+R` 刷新。终端按 `Ctrl+C` 停止。

> **别直接双击 `index.html`**。用 `file://` 打开时字体和部分资源可能加载不出来，看到的效果不准。

**测深色模式**：macOS 系统设置 → 外观 → 深色。网站会自动跟随。

**测手机端**：Chrome 里按 `Cmd+Option+I` 打开开发者工具，点左上角的手机图标。

---

## 2. 为什么之前"轻浮"——设计原则

这一节解释为什么现在的参数是这样，你改的时候心里有数。

我把 jonbarron.info 的实际渲染参数量出来，跟第一版做了对比：

| | jonbarron.info | 我的第一版（轻浮） | 现在 |
| --- | --- | --- | --- |
| 字体 | Lato | 系统字体（macOS 上是 SF Pro） | Lato |
| 正文字号 | 14px | 15px | 14.5px |
| **行距** | **normal（约 1.2）** | **1.62** | **1.45** |
| 正文颜色 | **纯黑 #000** | #1a1a1a + 两级灰 | **纯黑 + 一级灰** |
| 姓名 | 32px / **weight 400** | 34px / **600** / 负字距 | 32px / **400** |
| 章节标题 | 22px / **400** / **无下划线** | 21px / **600** / **有下划线** | 22px / **400** / 无下划线 |
| 论文标题 | 14px / **700** | 15px / 650 | 14.5px / **700** |
| 圆角 | **0** | 10px | **0** |
| 悬停动画 | **无** | 变背景色 + 放大 1.03 倍 | **无** |

**结论是四条原则。你以后自己改的时候，别破坏它们：**

### 原则一：全站只用两个字重

`400`（正文）和 `700`（论文/项目标题、人名、小标签）。**不要用 500、600 这种半粗**。

半粗在小字号下既不像正文也不像标题，会让所有元素"差不多重"，于是页面失去骨架 —— 这是"轻浮感"最主要的来源。Barron 全站唯一的加粗就是论文标题。

### 原则二：只用三种颜色

纯黑 `--fg`、一级灰 `--fg-muted`、链接蓝 `--link`。**不要加第四种。**

第一版用了三级灰（#1a1a1a / #5c5c5c / #8a8a8a），层次太多，整页对比度被稀释，看起来"发飘"。学术页面的重量感就来自高对比度的黑白。

### 原则三：行距要紧

正文 `1.45`。Barron 是约 1.2，紧到极致。行距一旦超过 1.6，文字块就散成一片，页面立刻"轻"起来。

如果你觉得现在读着累，**先加字号（`--size`）而不是加行距**。

### 原则四：不要装饰

无圆角、无阴影、无悬停动画、无大写小标签、标题下无横线。

这些都是 SaaS / 落地页的语汇。学术主页要的是"这是一份文件"的观感，装饰越少越可信。分隔层次靠**留白**和**字号**，不靠线条和色块。

> 唯一保留的"装饰"是选中论文的淡黄底色 `--highlight`（Barron 用 `#ffffd0`），因为它承载信息（标记代表作），不是纯装饰。**最多标 1～2 篇**，标多了就等于没标。

---

## 3. 调外观：stylesheet.css

所有可调参数都集中在文件最上面的 `:root { }` 里。**优先改这里，不要往下面翻着改。**

```css
:root {
  --max-width: 800px;        /* 版心宽度 */

  --font: "Lato", Verdana, Helvetica, Arial, sans-serif;
  --size: 14.5px;            /* 正文字号 */
  --lh: 1.45;                /* 行距 */
  --size-name: 32px;         /* 姓名 */
  --size-h2: 22px;           /* 章节标题 */

  --fg: #000000;             /* 正文黑 */
  --fg-muted: #4a4a4a;       /* 次要灰 */
  --link: #1772d0;           /* 链接蓝 */
  --link-hover: #f09228;     /* 悬停橙 */

  --bg: #ffffff;
  --rule: #dddddd;           /* 分隔线 */
  --highlight: #ffffd8;      /* 选中论文的底色 */
}
```

### 常见调整

**觉得字太小** → 改 `--size: 15px`。整站按比例变大，不用管别的。

**觉得还是太松** → 改 `--lh: 1.35`。别低于 1.3，中文读者会不适。

**想换回系统字体**（加载更快、不依赖 Google Fonts）：

1. `stylesheet.css` 里改成
   ```css
   --font: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
   ```
2. `index.html` 里删掉 `<head>` 中那三行 `fonts.googleapis.com` / `fonts.gstatic.com` 的 `<link>`
3. 系统字体观感偏"App 界面"，可以把 `--lh` 调到 `1.4` 补偿

**想换链接颜色** → 改 `--link`。深蓝 `#1a4f8a`、墨绿 `#1a6b52`、酒红 `#8a2f3f` 都比默认蓝更"学术"。改完记得同步改深色模式那一份（见下）。

**想让描述文字浅一点** → 在 `.blurb` 规则里加一行 `color: var(--fg-muted);`（现在是纯黑，跟 Barron 一致）。

### 深色模式

颜色变量一共写了**三份**，都在文件上半部分：

1. `:root { }` —— 浅色（默认）
2. `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) { } }` —— 跟随系统深色
3. `:root[data-theme="dark"] { }` —— 手动指定深色（目前没有切换按钮，留着备用）

**改任何颜色，三处都要改**，否则深色模式下会出现看不见的文字。

字号、行距、间距只在 `:root` 里定义一次，不需要重复。

---

## 4. 改内容：index.html

### 文件结构

```html
<head>          标题、SEO 描述、字体、favicon
<body>
  <header class="masthead">    姓名、头像、自我介绍、链接行
  <section id="research">      Research —— 手稿和独立项目
  <section id="conference">    Conference —— 会议报告
  <section id="experience">    Research Experience —— RA 经历
  <section id="education">     Education —— 学历
  <section id="toolkit">       Technical Skills —— 技术能力
  <footer>                     邮箱、致谢
```

每个 `<section>` 之间有 `<!-- ==== -->` 注释分隔，找位置很容易。

### 两种内容块

**A. 条目块**（论文、会议、项目用这个）—— 左图右文：

```html
<article class="item">
  <div class="thumb">
    <svg viewBox="0 0 320 200">…</svg>       ← 缩略图
  </div>
  <div>
    <span class="title">论文标题</span>
    <p class="authors"><span class="me">Juncheng Lu</span>*, Y. Zhang*</p>
    <p class="venue">Journal of XXX, <span class="status">under review</span></p>
    <p class="blurb">一两句话说明这篇做了什么。</p>
  </div>
</article>
```

- `class="item is-highlighted"` → 加淡黄底色，标记代表作
- `<span class="me">` → 把你自己的名字加粗
- `.blurb` 可以整行删掉（不是必须的）

**B. CV 行块**（经历、学历用这个）—— 左内容右日期：

```html
<div class="entry">
  <div>
    <div class="entry-role">Research Assistant</div>
    <div class="entry-where">实验室、学校、PI</div>
    <div class="entry-detail">一句话描述（可删）</div>
  </div>
  <div class="entry-when">Sept 2025 – present<br />New York, NY</div>
</div>
```

### 特殊字符

HTML 里不能直接打某些符号，要用转义写法：

| 你想要 | 就写 | 用在哪 |
| --- | --- | --- |
| — （破折号） | `&mdash;` | 句子里 |
| – （连接号） | `&ndash;` | 日期区间 `2022 &ndash; 2025` |
| · （间隔点） | `&middot;` | 并列项之间 |
| † （剑号） | `&dagger;` | 通讯作者 |
| & | `&amp;` | `Methods &amp; Tools` |
| 斜体 | `<em>文字</em>` | 强调 |

共同一作的 `*` 可以直接打，不用转义。

> ⚠️ **共同一作的 `*` 和通讯作者的 `&dagger;` 一定要保留**，页面底部有一行 `* Equal contribution. † Corresponding author.` 做说明。这对申请很重要，别嫌乱删掉。

---

## 5. 常见任务速查

### 加一篇新论文

1. 在 `index.html` 里找到 `<section id="research">`
2. 复制一整个 `<article class="item"> … </article>`（从 `<article` 到 `</article>`）
3. 粘到你想要的位置 —— **越新的越靠上**
4. 改标题、作者、期刊、描述
5. 缩略图先随便沿用一个，之后再换（见第 6 节）

### 给论文加 PDF / 代码链接

Barron 的做法是标题下面一行方括号链接。在 `.venue` 和 `.blurb` 之间插一行：

```html
<p class="links">
  <a href="data/paper.pdf">[PDF]</a>
  <a href="https://osf.io/xxxxx">[data &amp; code]</a>
  <a href="https://github.com/JunchengLu218/xxx">[code]</a>
</p>
```

PDF 放进 `data/` 文件夹，然后写 `data/文件名.pdf`。

### 把标题变成可点击的链接

```html
<!-- 改前 -->
<span class="title">论文标题</span>

<!-- 改后 -->
<a class="title" href="https://链接">论文标题</a>
```

样式表里已经写好了 `a.title` 的规则，会自动变成蓝色。

### 换头像

把照片存成 `images/profile.jpg` 覆盖现在那张灰色占位图。

- **必须接近正方形**，会被裁成圆形
- 建议 600×600 以上
- 不是 `.jpg` 的话，改 `index.html` 里 `<img class="portrait" src="images/profile.jpg"`

### 换 CV

把新 PDF 放进 `data/`，然后确认 `index.html` 里这一行指向对：

```html
<a href="data/Juncheng_Lu_CV.pdf">CV</a>
```

> ⚠️ 现在这份 PDF 里有你的**手机号**。PDF 一旦上线就是公开可下载的。建议专门做一版不含电话的"网页版 CV"。网页正文里我没有放电话。

### 补上 Scholar / LinkedIn

`index.html` 的链接行里有两个 `REPLACE_ME`：

```html
<a href="https://scholar.google.com/citations?user=REPLACE_ME">Scholar</a>
<a href="https://www.linkedin.com/in/REPLACE_ME">LinkedIn</a>
```

填真实链接，或者把不需要的整行连同前面的 `<span class="sep">/</span>` 一起删掉。

### 加一个新板块（比如 Teaching、Awards）

复制 `<section id="education">` 整块，改 `id`、改 `<h2>` 标题、改里面的 `.entry`。放在你想要的位置即可，不需要注册到任何地方。

### 加 News 板块

如果以后想加，放在 `<section id="research">` **前面**，用最简单的列表就行：

```html
<section id="news">
  <h2>News</h2>
  <div class="entry">
    <div>论文在 JESP 进入 R&amp;R</div>
    <div class="entry-when">2026-08</div>
  </div>
</section>
```

> 现在没加，是因为你的 news 内容（SANS、入学、R&R）跟 Publications 和 Experience 完全重合，加了会显得在凑数。等你有了真正的新消息（论文接收、拿到 offer）再加。

---

## 6. 示意图（SVG）怎么换

现在的四张缩略图是**直接写在 `index.html` 里的 SVG 示意图**（不是图片文件），画的都是对应研究实际用到的方法：

| 条目 | 示意图画的是 |
| --- | --- |
| Metacognitive accuracy | MPT 树（对应 MPT/Quad 模型） |
| Collateral-harm（R&R） | DDM 累积路径 + ERP 波形时间对齐（对应 hierarchical Bayesian DDM + P2/N2） |
| Social roles | 有/无社会角色信息时 civilian–soldier 差距的缩小 |
| Weapon identification | 潜变量参数 → 脑区激活（对应 DDM/MPT + fMRI） |

**换图时注意方法要对得上**——比如 Social roles 用的是 mixed-effects 模型，不是 DDM，就不该配一张 DDM 的图。

> ⚠️ **它们是方法示意，不是任何真实数据的图。** 别在任何地方暗示这是你的结果。有真图就尽快换掉 —— 真实的后验分布图、参数恢复图、ERP 波形，说服力比示意图高一个量级。

**换成真实图片：**

1. 图片放进 `images/`，比如 `images/ddm-posterior.png`
2. 找到对应的 `<div class="thumb">`，把里面**整个 `<svg>…</svg>` 删掉**，换成：

```html
<div class="thumb">
  <img src="images/ddm-posterior.png" alt="Posterior distributions of drift rate by condition" />
</div>
```

样式表里 `.thumb img` 已经写好了，会自动适应宽度。

**图片建议**：横向，约 3:2 比例（比如 640×400），PNG 或 JPG。缩略图显示宽度只有 160px，所以**图里别放小字**，看不清。

**SVG 里的颜色**：如果你想改示意图配色，用的是这几个变量（在 `stylesheet.css` 的 `:root` 里，深色模式下自动切换）：

- `var(--fig-bg)` 背景
- `var(--fig-ink)` 主线条（坐标轴、边界）
- `var(--fig-line)` 次要线条（虚线、辅助线）
- `var(--fig-a)` 蓝色数据线
- `var(--fig-b)` 橙色数据线

---

## 7. 发布到 GitHub Pages

纯静态网站，**不需要任何构建流程**，GitHub Pages 直接读根目录。

### 第一次发布

```bash
cd ~/Documents/Github/junchenglu-homepage
git remote add origin https://github.com/JunchengLu218/JunchengLu218.github.io.git
git push -u origin main --force
```

> `--force` 会**覆盖**仓库里现有的 al-folio 内容。旧内容在 GitHub 的历史里还能找回，但请先确认你确实不要它了。

然后去 GitHub 仓库 → **Settings → Pages**：

- **Source**：`Deploy from a branch`
- **Branch**：`main` / `(root)`

保存后等 1～2 分钟，访问 <https://junchenglu218.github.io/>。

> 旧仓库现在走的是 al-folio 的 GitHub Actions 部署。切成 "Deploy from a branch" 之后，`.github/workflows/` 里那些文件就没用了，可以删掉。

### 以后每次更新

```bash
cd ~/Documents/Github/junchenglu-homepage
git add -A
git commit -m "描述改了什么"
git push
```

推上去大约 1 分钟后生效。**如果没变化**：`Cmd+Shift+R` 强制刷新（浏览器缓存了旧的 CSS）。

---

## 8. 出错了怎么办

### 页面样式全乱 / 变成纯文字

`stylesheet.css` 里有语法错误，通常是少了 `}` 或 `;`。CSS 出错不会报错，只会静默失效。

**排查**：Chrome 里 `Cmd+Option+I` → Console 标签页看有没有红字。

### 某段文字消失了

`index.html` 里标签没闭合，比如 `<p>` 忘了写 `</p>`，或者 `<article>` 少了 `</article>`。浏览器会把后面的内容吞掉。

**排查**：把改动前后的文件对比一下 —— `git diff`。

### 图片显示不出来

- 路径写错（区分大小写！`Profile.jpg` ≠ `profile.jpg`）
- 文件不在 `images/` 里
- 用 `file://` 直接打开的 —— 改用第 1 节的本地服务器

### 深色模式下有文字看不见

改颜色时只改了 `:root`，忘了改另外两份。见第 3 节末尾。

### 一键回到上一个能用的版本

```bash
cd ~/Documents/Github/junchenglu-homepage
git checkout -- index.html stylesheet.css
```

这会丢弃**所有未提交**的改动，回到上次 `git commit` 的状态。

**养成习惯**：每改完一件事就提交一次，出问题随时能退回去。

```bash
git add -A && git commit -m "加了新论文"
```

查看历史：`git log --oneline`

---

## 附：发布前检查清单

- [ ] `images/profile.jpg` 换成真实头像（现在是灰色占位图）
- [ ] 两个 `REPLACE_ME` 填上真实链接，或删掉整行
- [ ] CV PDF 决定是否去掉手机号（现在这版里有 773-328-0805）
- [ ] 决定要不要保留会议那条的「unable to present due to visa restrictions」
- [ ] 手机上打开看一遍
- [ ] 深色模式看一遍
- [ ] 所有链接点一遍，确认没有 404
